<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.partTime">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoginActivity" />
        <activity android:name=".activity.MainActivity" />
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.SettingActivity" />
        <activity android:name=".activity.XiugaiPswActivity" />
        <activity android:name=".activity.FindPswActivity" />
        <activity android:name=".activity.RenwuActivity" />
        <activity android:name=".activity.SousuoActivity" />
        <!-- <activity android:name=".activity.SaoActivity" /> -->
        <activity android:name=".activity.GengduoActivity" />
        <activity android:name=".activity.JopTiem" />
        <activity android:name=".activity.MingPianActivity" />
    </application>

</manifest>